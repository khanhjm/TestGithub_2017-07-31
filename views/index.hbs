<!DOCTYPE html>
<html>
<head>
    <title>Handlebars</title>
    <meta charset="utf-8">
</head>
    <body>
        {{#each users as |user|}}
        <div>
            <img src="/profilepics/{{user.username}}_sm.jpg" alt="hình ảnh">
            <a href="/{{user.username}}">{{user.name.full}}</a>
        </div>
        {{/each}}
    </body>
</html>